# --- 로컬 전용 연구 자료 (공개 저장소에 올리지 않는다) ---------------------
# 사내 코드베이스 분석, 미공개 안전결함 기술서, 저작권 도서에서 추출한 요약을
# 포함한다. 의도적으로 로컬에만 둔다.
background/
스킬-제안서.md

# --- 세션 상태 / 에이전트 메모리 ------------------------------------------
# 앞에 슬래시가 없으므로 하위 디렉터리의 .remember/ 도 함께 매칭된다.
.remember/
.claude/settings.local.json

# --- Claude Code 플러그인 개발 부산물 --------------------------------------
*.log

# --- Node (JS 훅 / MCP 서버용) ---------------------------------------------
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
.env
.env.*
!.env.example

# --- Python (Python 훅용) ---------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/

# --- 에디터 / OS ------------------------------------------------------------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
